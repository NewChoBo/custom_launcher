import 'package:flutter/material.dart';

class FrameLayout extends StatelessWidget {
  const FrameLayout({super.key});

  @override
  Widget build(BuildContext context) {
    return const Padding(padding: EdgeInsets.all(8.0), child: Column());
  }
}
